<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
{{{ head-html }}}
{{#if rem-size}}
    <style>
        body {
            font-size: {{rem-size}}px
        }
    </style>
{{/if}}
{{#if primary-color}}
    {{!-- The bootstrap css variables, a start --}}
    <style>
        :root {
            --bs-primary: {{primary-color}};
        }
        .btn-primary {
            --bs-btn-bg: {{primary-color}};
            --bs-btn-border-color: {{primary-color}};
        }
    </style>
{{/if}}
